# Frank's Knowledge System

Frank 的个人知识系统，核心目标是积累知识、形成自己的框架与体系。

---

## 文件夹结构

- `00_Self/` — 自我层：自我画像 + 成长计划。整个知识系统的起点。
  - 自我画像（`个人总结_`、`AI总结_` 开头）
- `01_Plan/` — 计划层：从自我画像推导出的行动计划。
  - `驱动层.md` — 紧迫/不紧迫任务队列，实时更新
  - `流水账/` — 每周一份，Claude 自动追加每日记录
- `02_Inbox/` — 原始材料：文章、播客、clippings、访谈等。只读，不改动，用于溯源。
- `03_Work/` — Frank 自己的内容：总结、梳理、洞见、框架
  - `job-search/` — 数据分析求职相关
  - `courses/` — 学校课程
  - `reading/` — 读书笔记
  - `explore/` — 兴趣探索（金融、AI、历史、商业等）
- `04_Wiki/` — 精华提炼层：经过多次讨论验证、可跨场景迁移的思维框架
  - 来源于 `03_Work/` 的笔记，但已脱离具体文档，可独立使用
  - 框架由 AI 提候选 + Frank 审核确认后方可写入，不能由 AI 单独写入

---

## 工作流

### 循环一：学习（最终产出"判断"）

```
内容（视频/播客/文章）→ Markdown → 存入 02_Inbox / 03_Work
        ↓（03_Work/ 有对应文件夹放那里；没有放 02_Inbox/）
Claude 生成结构式总结 + 叙述式总结 → 03_Work/    ← 产出「理解」（主语：内容）
        ↓
Frank 边学习边提问（对话讨论）
        ↓
/train 苏格拉底式提问                              ← 产出「判断」（主语：我）
        ↓
判断按形状分流到四个去处：
  · 立场 / 修正（针对这篇）  → 回写原笔记（## 训练洞见）
  · 可迁移的判断 + 它的边界  → 04_Wiki/（Frank 审核后写入）
  · 新关联                  → 笔记之间建双链
  · 缺口（我还没想清楚的）   → 01_Plan 驱动层（下一步学什么）
        ↑_______________________________________↑
                 下次从被训练过的版本出发 = 复利
```

**结构式总结**：按主题和概念对内容做整体拆解——是什么、有哪几条、分哪几层。目的是快速抓住内容的"形状"，方便检索和扫描。

**叙述式总结**：按原内容的实际顺序逐段走——第一节讲什么、逻辑是怎么建立的，第二节讲什么、论点是怎么 prove 的，以此类推。忠实于原材料的展开路径，重点是每一部分的论证过程。不是把要点改写成句子，是按顺序跟着原内容走一遍。

### 循环二：计划

```
00_Self 自我画像 + 成长计划
        ↓
01_Plan 驱动层（紧迫 / 不紧迫队列）
        ↓
每天：选轨道 → 选任务 → 启动学习循环
        ↓
对话结束：Claude 总结 → Frank 确认 → 写入流水账
        ↓
每周：回顾流水账，更新驱动层状态
```

---

## AI 行为规则

- Frank 本人构建知识，AI 只是辅助——不要替他下结论，不要过度总结
- 对话中发现重要洞察时，主动说"这个值得记一下"，让 Frank 用自己的话写下来
- 新笔记默认建议放 `03_Work/` 对应主题；原始材料放置：`03_Work/` 有对应文件夹时放那里，没有则放 `02_Inbox/`
- 不强迫分类，跨主题的内容放最相关的文件夹即可

### 对话结束规则

plan 循环是三层、两个时间尺度的级联——**自下而上回填**（每次对话发生的事，逐层向上影响计划），但**越往上改得越少**：流水账每次记，驱动层每次顺手更新，成长计划只在攒够时才动。

每次对话即将结束时，Claude 主动按顺序走一遍**收尾清单**，并把结果告诉 Frank（让他不用猜哪步做了没做）：

**1. 流水账（必做）**
- 自己总结今天做了什么（一句话），等 Frank 确认后写入 `01_Plan/流水账/本周文件.md`
- 格式：`MM/DD：[总结内容] → [[产出文件1]] [[产出文件2]]`

**2. 驱动层（必做）**
驱动层是所有 `_plan` 子笔记的**调度板 / 索引**——每条计划用双链指向子笔记，本身只留一句状态。
- **联动同步（Claude 的活）**：本次对话改动过的 `_plan` 子笔记，把驱动层里它那一行的状态同步过来，保持索引与子笔记一致
- **增删取舍（Frank 的活）**：哪些计划继续 / 不继续，由 Frank 自己维护；Claude 不替他增删条目、不替他判断休眠
- 顺手更新任务状态：推进 / 完成（打勾）/ 新冒出的缺口（新增条目）
- 日常的勾选和加小任务，**停在这一层就好，不往上传**

**3. 成长计划（按条件触发，不是每次都改）**
- 只在两种情况提议更新 `00_Self/成长计划_plan.md`，其余跳过、不动它：
  1. 某个 **Phase 的任务做完了** → 推进对应 Phase 的状态
  2. 冒出的缺口 **大到改变方向**（不是「今天没做 X」，而是「发现真正缺的是 Y，要加一条新轨」）
- 触发时只**提议**，等 Frank 确认后再改；没触发就明确说「成长计划无需改动」

Frank 不需要主动提醒，Claude 每次对话结束都应主动走完这三步并报告结果。

### /train 产出规则

**train 是什么**：读、AI 总结、个人总结产出的是「理解」（主语是内容/作者）；/train 是唯一产出「判断」的一步（主语是「我」）。它用外部追问戳破"我懂了"的错觉，把理解逼成能用的判断。判断力正是 Frank 要建的实力，所以这一步的产物最该被认真对待。

**判别器**：一条能用「我……」开头 → 是 train 产物；只能用「作者说…／这篇讲…」开头 → 是总结，不收。

**train 产物的五种形状**（每条都以"我"开头）：

1. 立场：我认为 X（站哪边、为什么）
2. 边界：这个框架在 Z 时失效
3. 修正：我之前以为 A，其实是 B
4. 关联：这跟 W 是同一个道理
5. 缺口：我还没想清楚 Y

**训练中**：当 Frank 说出一条这样的判断时（尤其修正、边界、新关联），立刻问："这条是新判断，记吗？"

**训练结束**：把标记过的判断按去处分流——

- **可迁移的判断 + 它的边界** → 候选进 `04_Wiki/` 工具箱。"可迁移"＝能不能拿去问一个这篇笔记里没出现过的新对象。门槛：可迁移 + 有边界 + ≥2 个独立来源撞上同一判断（单源先留候选）。
- **只关于这一篇的立场/修正** → 回写原笔记 `## 训练洞见`。
- **关联** → 在相关笔记间建 `[[双链]]`。
- **缺口** → 追加到 `01_Plan/驱动层.md`，作为下一步学什么。

**原笔记没有 `## 训练洞见` 区块时，在文件末尾新建：**

```markdown
## 训练洞见

- YYYY-MM-DD：[以"我"开头的判断]
```

---

## 文件格式规则

Obsidian 知识库**只存放 Markdown 文件（.md）**。原因：非 Markdown 文件会拖慢 Obsidian 启动速度。

**非 Markdown 文件一律保存到桌面：**
- 默认路径：`/Users/mahaoran/Desktop/`
- 包括：`.pptx`、`.xlsx`、`.docx`、`.pdf`、脚本、临时文件等所有非 .md 格式

**例外：** 如果 Frank 明确指定了其他路径，按他说的来。

---

## 文件命名规范

`02_Inbox/` 和 `03_Work/` 里的文件，在文件名最前面标注内容类型：

- `原材料_` — 原始内容（访谈、文章、播客等），适用于 02_Inbox/ 和 03_Work/
- `AI总结_` — Claude 或其他 AI 整理的内容
- `个人总结_` — Frank 自己写的笔记、框架、分析
- `train_` — `/train` 技能训练产生的对话内容

`03_Work/` 各主题文件夹内的专项计划文件，使用 `_plan` 后缀：

- 格式：`主题名_plan.md`
- 示例：`毛泽东决策研究_plan.md`、`SQL训练_plan.md`

示例：`AI总结_李想访谈要点.md`、`个人总结_商业模式框架.md`、`train_知识系统_20260531.md`

---

## 双链规范

Obsidian 双链一律用**文件名**，不要用路径。

- ✅ `[[指标体系答题模板]]`
- ✅ `[[指标体系答题模板|答题模板]]`（带显示别名时同理，只用文件名）
- ❌ `[[03_Work/job-search/数据分析/面试methods/指标体系答题模板]]`

原因：路径式双链在文件移动或文件夹重命名后会断；文件名双链由 Obsidian 自动解析，移动文件不断链，重命名时还能自动更新所有引用。

- **前提**：文件名保持库内唯一（同名文件会导致双链歧义）。
- **例外**：Templater 模板里的占位符（如 `{{title}}`）不受此约束。
