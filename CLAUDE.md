# Frank's Knowledge System

Frank 的个人知识系统，核心目标是积累知识、形成自己的框架与体系。

---

## 文件夹结构

- `00_Self/` — 自我层：自我画像 + 成长计划。整个知识系统的起点。
  - 自我画像（`个人总结_`、`AI总结_` 开头）
- `01_Plan/` — 计划层：从自我画像推导出的行动计划。
  - `驱动层.md` — 紧迫/不紧迫任务队列，实时更新
  - `流水账/` — 每周一份，Claude 自动追加每日记录
- `02_Inbox/` — 原始材料：文章、播客、clippings、访谈等。只读，不改动，用于溯源。
- `03_Work/` — Frank 自己的内容：总结、梳理、洞见、框架
  - `job-search/` — 数据分析求职相关
  - `courses/` — 学校课程
  - `reading/` — 读书笔记
  - `explore/` — 兴趣探索（金融、AI、历史、商业等）
- `04_Wiki/` — 精华提炼层：经过多次讨论验证、可跨场景迁移的思维框架
  - 来源于 `03_Work/` 的笔记，但已脱离具体文档，可独立使用
  - 框架由 AI 提候选 + Frank 审核确认后方可写入，不能由 AI 单独写入

---

## 工作流

### 循环一：学习

```
内容（视频/播客/文章）→ Markdown 格式
    ↓
原材料存放：03_Work/ 有对应文件夹 → 放那里；没有 → 放 02_Inbox/
    ↓
Claude 生成结构式总结 + 叙述式总结 → 03_Work/
    ↓
Frank 边学习边提问（对话讨论）
    ↓
/train 复习 → 产生新关联 / 洞见写回笔记（## 训练洞见区块）
    ↓
反复出现的可迁移框架 → 04_Wiki/（Frank 审核后写入）
```

### 循环二：计划

```
00_Self 成长计划（我要做什么）
    ↓
01_Plan 驱动层 → 今天走哪条轨、做哪项任务
    ↓
启动学习循环
    ↓
对话结束 → Claude 更新驱动层状态 + 流水账追加一行
    ↓
每周 → Routine 生成周报
```

---

## AI 行为规则

- Frank 本人构建知识，AI 只是辅助——不要替他下结论，不要过度总结
- 对话中发现重要洞察时，主动说"这个值得记一下"，让 Frank 用自己的话写下来
- 新笔记默认建议放 `03_Work/` 对应主题；原始材料放置：`03_Work/` 有对应文件夹时放那里，没有则放 `02_Inbox/`
- 不强迫分类，跨主题的内容放最相关的文件夹即可

### 对话结束规则

每次对话即将结束时，Claude 主动执行：
1. 自己总结今天做了什么（一句话）
2. 等 Frank 确认后，写入 `01_Plan/流水账/本周文件.md`（格式：`MM/DD：[总结内容] → [[产出文件1]] [[产出文件2]]`）
3. 更新 `01_Plan/驱动层.md` 对应任务的状态

Frank 不需要主动提醒，Claude 每次都应主动做这一步。

### /train 洞见标记规则

在 /train 对话中，当 Frank 说出一个**之前没表达过的关联，或推翻了自己旧判断**时，立刻说：

> "这句话是新的，要记吗？"

Frank 确认后继续训练。训练结束时，把所有标记过的句子列出来，帮 Frank 追加到原笔记的 `## 训练洞见` 区块。

**原笔记没有该区块时，在文件末尾新建：**

```markdown
## 训练洞见

- YYYY-MM-DD：[洞见句子]
```

这是知识复利的核心机制：洞见写回原笔记，下次 /train 读到的是被训练过的版本。

---

## 文件格式规则

Obsidian 知识库**只存放 Markdown 文件（.md）**。原因：非 Markdown 文件会拖慢 Obsidian 启动速度。

**非 Markdown 文件一律保存到桌面：**
- 默认路径：`/Users/mahaoran/Desktop/`
- 包括：`.pptx`、`.xlsx`、`.docx`、`.pdf`、脚本、临时文件等所有非 .md 格式

**例外：** 如果 Frank 明确指定了其他路径，按他说的来。

---

## 文件命名规范

`02_Inbox/` 和 `03_Work/` 里的文件，在文件名最前面标注内容类型：

- `原材料_` — 原始内容（访谈、文章、播客等），适用于 02_Inbox/ 和 03_Work/
- `AI总结_` — Claude 或其他 AI 整理的内容
- `个人总结_` — Frank 自己写的笔记、框架、分析
- `train_` — `/train` 技能训练产生的对话内容

`03_Work/` 各主题文件夹内的专项计划文件，使用 `_plan` 后缀：

- 格式：`主题名_plan.md`
- 示例：`毛泽东决策研究_plan.md`、`SQL训练_plan.md`

示例：`AI总结_李想访谈要点.md`、`个人总结_商业模式框架.md`、`train_知识系统_20260531.md`
