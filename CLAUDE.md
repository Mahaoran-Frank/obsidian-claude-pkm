# 个人知识系统

核心目标：积累知识，形成自己的框架与体系。

---

## 文件夹结构

- `00_Self/` — 自我层：自我画像 + 成长计划 + 每日日记。整个知识系统的起点。
  - 自我画像（`个人总结_`、`AI总结_` 开头）
  - `Plan.md` — 每日流水账
  - `Journal/` — 每日快照（手动记录）
- `01_Inbox/` — 原始材料：文章、播客、clippings、访谈等。只读，不改动，用于溯源。
- `02_Work/` — 自己的内容：总结、梳理、洞见、框架（子文件夹按主题自行划分）
- `03_Wiki/` — 精华提炼层：经过多次讨论验证、可跨场景迁移的思维框架
  - 来源于 `02_Work/` 的笔记，但已脱离具体文档，可独立使用
  - 框架由 AI 提候选 + 本人审核确认后方可写入，不能由 AI 单独写入

---

## 工作流

```
00_Self（我是谁）→ 成长计划（我要做什么）→ 确定学习方向
    ↓
01_Inbox（原材料输入）→ 02_Work（消化产出）
    ↓
/train 知识对话训练 → 产生新关联 / 发现逻辑漏洞
    ↓
洞见写回原笔记（## 训练洞见区块）→ 下次训练起点更高
    ↓
任务追踪 → 各主题子 plan 文件
    ↓
每日快照 → 00_Self/Journal/（手动记录）
    ↓
跨文档反复出现的可迁移框架 → 03_Wiki/（本人审核后写入）
```

---

## 项目计划

每学一个主题，在 `02_Work/` 对应文件夹下建立自己的 plan 文件，不需要统一归集到"活跃池"。

- 深度学习方向用独立计划文件，分阶段写清楚目标、任务和完成标志（参考 `job-search/AI产品运营/学习计划.md` 的格式）
- 简单主题可用 `_plan.md`，分三层：**进行中 / 待启动 / 已完成**

---

## 00_Plan 架构

- `Plan.md` — 每日流水账：记录当天的学习内容和进度，按日期组织。
- `Journal/YYYY-MM-DD.md` — 每日手写快照，有需要时手动创建。

---

## AI 行为规则

- 本人构建知识，AI 只是辅助——不要替用户下结论，不要过度总结
- 对话中发现重要洞察时，主动说"这个值得记一下"，让用户用自己的话写下来
- 新笔记默认建议放 `02_Work/` 对应主题，原始材料放 `01_Inbox/`
- 不强迫分类，跨主题的内容放最相关的文件夹即可

### /train 洞见标记规则

在 /train 对话中，当用户说出一个**之前没表达过的关联，或推翻了自己旧判断**时，立刻说：

> "这句话是新的，要记吗？"

用户确认后继续训练。训练结束时，把所有标记过的句子列出来，追加到原笔记的 `## 训练洞见` 区块。

**原笔记没有该区块时，在文件末尾新建：**

```markdown
## 训练洞见

- YYYY-MM-DD：[洞见句子]
```

这是知识复利的核心机制：洞见写回原笔记，下次 /train 读到的是被训练过的版本。

---

## 文件命名规范

`02_Work/` 里的文件，在文件名最前面标注内容类型：

- `原材料_` — 原始内容（访谈、文章、播客等），通常同步存 `01_Inbox/`
- `AI总结_` — Claude 或其他 AI 整理的内容
- `个人总结_` — 自己写的笔记、框架、分析
- `train_` — `/train` 技能训练产生的对话内容

示例：`AI总结_李想访谈要点.md`、`个人总结_商业模式框架.md`、`train_知识系统_20260531.md`
