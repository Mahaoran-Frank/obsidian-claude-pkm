# 设计文档 —— 为什么这么设计

> 这份是给**想深究"为什么这么设计"的人**看的（设计理念、两个循环的完整推导、关键取舍）。
> 只想上手用，看 [README.md](README.md) 就够。
> AI 助手的**行为配置**在精简的 `CLAUDE.md`（只装会改变 AI 行为的规则）。
> 分工原则：**会改变 AI 行为的 → `CLAUDE.md`；解释系统给人懂的 → 这里。**

---

## 核心理念

核心目标只有一个：积累知识、形成自己的框架与体系。

- **复利 = 把知识点彼此连起来、网越织越大**；越学，能接上的越多。
- **连接本身就是产物**——不强求每轮逼出一条「判断」。
- **人是主体，AI 只是辅助**——复利必须由使用者自己构建产生，长在他的脑子和笔记网里，不在 AI 的输出里。（这条是整套系统的第一性目的，见 `CLAUDE.md` 顶部。）

---

## 两个循环

### 循环一：学习（产出知识连接 + 新方向）

> 实际执行由 `vault-ingest` skill 承载（取文 → 双总结 → Opus 种子 → 收尾）。

```
   ┌──────────────────────────────────────────────────────────┐
   ↓                                                          │
内容（视频/播客/文章）→ Markdown → 存入 02_Inbox / 03_Work       │
        │（03_Work/ 有对应文件夹放那里；没有放 02_Inbox/）         │
        ↓  结构式总结 + 叙述式总结    → 产出「理解」（主语：内容）   │
        │                                                     │
        ↓  和 AI 讨论 → 个人总结      → 消化成自己的话            │
        │                                                     │
        ↓  Opus 列浅图：哪里可补充、哪里和库里已有内容有关联        │
        │                                                     │
        ↓  /tree 顺着浅图往深里探      → 长出新东西                │
        │                                                     │
        ↓  tree 产物按形状分流：                                 │
        │    · 新研究方向（一轮聊不完的坑）→ 01_Plan 驱动层        │
        │    · 关联为真               → 笔记之间建双链           │
        │    · 关联为假               → 改图（杀掉假连接也是产物） │
        │    · 待续没挖完             → backlog / 并进对应 _plan  │
        │    · 安静副产品：冒「我…」判断 → 回写原笔记 ## 训练洞见   │
        │                                                     │
        └──── 下次从「连得更密的版本」出发 ＝ 复利 ────────────────┘
```

**结构式总结**：按主题和概念对内容做整体拆解——是什么、有哪几条、分哪几层。目的是快速抓住内容的"形状"，方便检索和扫描。

**叙述式总结**：按原内容的实际顺序逐段走——第一节讲什么、逻辑怎么建立，第二节讲什么、论点怎么 prove，以此类推。忠实于原材料的展开路径，重点是每部分的论证过程。不是把要点改写成句子，是按顺序跟着原内容走一遍。

### 循环二：计划 —— 让方向不发散

**设计原则**：计划系统要干三件本质不同的事——**方向**（该往哪走）、**调度**（现在做哪件）、**记忆**（之前做到哪）。三件事各有合适的形状，别塞进同一个文件互相打架。而且贯穿一条铁律，和知识循环同构：**人是主体，AI 辅助——AI 让"挑"省力，但绝不替使用者挑。**

**五个元素，从慢到快：**

```
北极星（自我画像，几乎不动）
  └─ 成长计划（几个战略赌注 + 为什么，纯战略，攒够才动）
       └─ 各 plan（深执行细节 + 断点状态；大 plan 用双链挂子 plan）
            └─ 驱动层 = 一张薄全局板（每个活跃 plan 一行：断点 + 下一步 + 信号）
                 → 使用者扫板、对着战略自己挑今天干哪件
流水账（做完的成果，一行 + 双链，按时间排的产出索引）
```

- **方向**：北极星说"我是谁"，成长计划把它翻成几个战略赌注。学什么从"我是谁"推下来，不是随机捡。
- **调度（治"不知道每天干啥"）**：驱动层是一张**能一眼扫完的薄板**，不是一堵段落墙。使用者扫板掌握全局、自己挑——**AI 只负责把板维护准，绝不替他挑/排**。深细节在各 plan 文件里，板上只放一行索引。
- **记忆（治"半成品丢失、隔代 AI 接不上"）**：每个没做完的 plan 身上挂一个**断点**（现在到哪 + 下一步）。它是**存档点**，不是成果：AI 收尾时自动写（半成品也写）、下次开场读它续上、做完即清。**成果归档（流水账）和断点存档是两回事**——前者等做完、永久；后者随时、用完即弃。这样使用者不必手动沉淀半成品，隔代对话也接得上。

**两个循环在驱动层咬合**：计划循环里挑出的任务交给学习循环；学习循环跑出的「缺口」回到驱动层，成为下一步的候选。

---

## 关键设计决策

- **04_Wiki 靠主动输入、AI 不索取**：思维框架提炼层。**框架是大量实例反复撞出后的"晚期析出物"**，强行每轮索取只会逼出 n=1 的假框架——所以这一层只在使用者自己说"这个想写成框架"时由他亲手写入，AI 不提醒、不主动往这里引导。

- **train → tree，并重定位**：原 `/train`（苏格拉底式逐句逼问、把一篇榨成判断）已退役。现在的 `tree` 重定位为**对话分叉管理器**：一段会不断岔出新枝的讨论里，把每根岔枝轻量记下、挂住，护住主线，并维护一张"主线走哪、岔了哪些枝"的图。

- **判断降为安静副产品**：这套系统里"知识连接 > 逼出个人判断"。使用者自己冒出的「我…」判断，逐字抓、不拔高，回写笔记的 `## 训练洞见`。
